# 星播 iOS 云端编译版

当前版本 0.3.5，云端构建号 21，Bundle ID 沿用 `app.eggplant1907.jet4469`。Codemagic 会直接读取 `pubspec.yaml` 中的版本号与构建号。

这是 Codemagic 云端编译源码，不是已签名 IPA。普通页面保持竖屏，点击播放器右下角全屏按钮后进入横屏；播放器包含选集、下一集、弹幕、倍速、画中画、播放记忆、片头片尾和定时关闭，并保留现有播放列表过滤功能。

1. 解压源码 ZIP，将内容更新到现有 GitHub 仓库根目录。`pubspec.yaml`、`codemagic.yaml`、`ios`、`lib`、`third_party` 应在同一级。务必一起上传 `third_party/video_player_avfoundation`，保留 LICENSE。
2. 在现有 Codemagic 应用选择仓库里的 `codemagic.yaml`。
3. 可先运行 `xingbo-ios-verify`：不需要证书，运行测试并用 Xcode 编译。生成的 `unsigned-Runner.zip` 不能直接安装。
4. 安装版选择 `xingbo-ios-adhoc`。沿用已配置且未过期的 Apple Distribution 证书与 Ad Hoc 描述文件；没有配置时在 Codemagic 签名设置上传 `.p12` 与匹配的 `.mobileprovision`，填写证书密码。描述文件须包含手机 UDID。不要把证书或密码上传到 GitHub。
5. 签名的 Bundle ID 必须匹配；不同则同时修改 `codemagic.yaml` 和 `ios/Runner.xcodeproj/project.pbxproj` 中的应用 Bundle ID。
6. 成功后从 Artifacts 下载 IPA，安装到描述文件包含的设备。

## 本次修改

构建 16 恢复无标记插播识别，同时收紧误删保护。除了成对 `CUE-OUT`/`CUE-IN` 明确标记的广告，只有满足“长连续正片编号、短独立连续编号、随后精确接回正片下一编号”的片段才会删除；单独的文件名变化、序号中断、CDN 变化、时长和 `DISCONTINUITY` 都不足以触发删除。已用真实播放列表验证两段插播均可识别，并保留插播前后的连续正片。

去插播只在 M3U8 片段层工作：跳过独立插入、占满画面的广告视频片段。正片画面里的角落水印、台标、字幕和画中广告保持原样；应用不裁剪、不遮挡，也不修改视频画面。

构建 14 让横屏顶部返回、底部退出全屏和系统返回都先等待切换到竖屏，再关闭全屏页面。主动退出后，当前详情页不再自动弹回横屏；播放器内的全屏按钮仍可手动再次进入横屏，离开详情页时解除方向限制。

参考腾讯视频手机端交互：横屏沉浸播放，播放中操作栏 3 秒自动隐藏；轻点显示或隐藏，双击播放或暂停，左右滑动调整进度，长按临时 3 倍速、松手恢复，支持锁屏防误触。画面保持原比例，不裁切字幕。参考资料是历史手机端教程，不代表已复刻最新版腾讯视频全部功能。

播放器普通与全屏播放结束后自动进入当前线路下一集并从头继续播放；最后一集结束后停止。暂停、缓冲和错误不会触发跳集。iOS 画中画中结束一集会关闭当前画中画并加载下一集，不会自动重开系统画中画或强制唤起应用。安卓原生画中画通过播放位置轮询检查片尾及结束边界，可能延后约一秒，仍需真机验证。

底部“更多”新增“片头片尾”和“定时关闭”。片头片尾默认关闭，按每部影片、每个账号单独记忆设置的秒数，每次调整 5 秒，范围 0–300 秒；设置后自动跳过，不做画面或音频识别。过短视频不执行可能跳过整集的设置，手动回看已跳过的片头不会被反复跳走。

定时关闭支持播完本集、播完两集（含本集），或 15、30、60、90 分钟后停止。分钟计时包含暂停时间；手动换集不计入已看完的集数，跳片尾计入本集结束。定时停止优先于自动下一集，暂停并保存进度，不强退应用；可随时取消或改选，重启应用不恢复旧定时。系统画中画与后台计时仍需真机验证。

每个视频单独分析插播结构，广告位置和时长不写死。苹果端通过仅绑定 `127.0.0.1`、带随机路径的本地服务交付过滤后的 m3u8；视频片段从原源加载，没有第三方解析接口。无法可靠识别、加密、多音轨等未支持结构保留原播放。

原画中画会创建第二个 AVPlayer，重新加载、跳转当前进度；本次改为直接复用正在播放的 AVPlayer 和已有缓冲。进入和返回不重新加载、不跳转、不切换音频流，保留倍速。关闭画中画则暂停。

本地修改的 AVFoundation 插件源码放在 `third_party`，保留上游 BSD 许可证，修改说明为 `XINGBO_CHANGES.md`。不用私有 Apple API，不对共享依赖缓存打补丁。安卓继续使用原先的播放与画中画实现。

## 验证边界

本机 55 项自动化测试通过，包含选集交互、控制栏显隐与手势、去插播时间换算、苹果本地播放列表服务、画中画通道参数与错误处理，以及片头片尾跳过、两集计数、定时停止优先级和进入画中画时到期的竞争情况。

Windows 可运行 Dart 测试、静态分析及本地播放列表服务测试，无法运行 Xcode。此交付未执行云端构建，也没有完成 iPhone 真机播放、后台与画中画流畅性验证；不能保证消除网络本身造成的卡顿。云端编译配置固定 Flutter 3.47.4，使用现有 iOS 项目和签名方案。
